// This file is reserved for React Query hooks that interact with the backend.
// Currently, the backend has no methods, so no queries are needed.
// Add backend query hooks here as the backend API grows.

export {};
